import 'package:flutter/material.dart';

class AppThemes {
  static ThemeData themeLight() {
    return ThemeData(
      textTheme: TextTheme(
        bodyText1: TextStyle(fontSize: 18),
      ),
    );
  }
}
